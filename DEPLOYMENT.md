# راهنمای کامل استقرار ربات سیگنال (Signal Bot)

این فایل، خلاصه‌ی کامل چیزی است که طی چند روز کار، آزمون‌وخطا، و رفع اشکال به آن رسیدیم.
هدف: اگر یک روز خواستید این ربات را روی هاست دیگری (رایگان یا پولی) دوباره بالا بیاورید،
فقط کافی است همین مراحل را دنبال کنید — بدون نیاز به تکرار مسیر سخت قبلی.

## ۱. فایل‌های لازم

این ۵ مورد باید حتماً با هم منتقل شوند:

- `main.py` — کد اصلی ربات (نسخه‌ی نهایی و کامل)
- `requirements.txt` — لیست کتابخانه‌های لازم
- `assets/background.png` — تصویر پس‌زمینه‌ی گزارش عملکرد
- `fonts/DejaVuSans-Bold.ttf` — فونت عنوان گزارش
- `fonts/DejaVuSansMono-Bold.ttf` — فونت متن مونواسپیس گزارش

⚠️ بدون پوشه‌های `assets` و `fonts`، بخش ساخت تصویر گزارش عملکرد (خلاصه هر ۵ سیگنال) کار نمی‌کند.

## ۲. متغیرهای محیطی (Environment Variables) لازم

هرجا که دیپلوی می‌کنید (Railway یا هر جای دیگر)، این سه متغیر را باید تنظیم کنید:

| نام متغیر | توضیح |
|---|---|
| `BOT_TOKEN` | توکن ربات از BotFather |
| `CHANNEL_ID` | آیدی عددی کانال VIP که سیگنال‌ها و اعلان‌ها در آن پست می‌شوند |
| `ADMIN_ID` | آیدی عددی تلگرام ادمین (تنها کسی که می‌تواند سیگنال بفرستد) |

## ۳. معماری و ویژگی‌های کلیدی (خلاصه‌ی کاری که انجام شده)

- **فرمت پست سیگنال:** جفت‌ارز، نوع (LONG/SHORT)، یک یا دو Entry، تا ۵ تارگت (با آیکون‌های ماه 🌑🌘🌗🌖🌕)، لوریج، درصد بالانس، Stop Loss — همه با دکمه‌های تعاملی برای تعداد متغیر Entry/تارگت
- **پایش خودکار قیمت:** هر ۲۰ ثانیه، قیمت از **BingX Futures/Perpetual** (نه Spot، چون سیگنال‌ها لوریجی‌اند) خوانده می‌شود
- **چرا BingX و نه Binance/CoinGecko:** Binance از سرورهای Railway مسدود است (خطای ۴۵۱)؛ CoinGecko تاخیر و هشدار کاذب داشت؛ BingX سریع‌تر و قابل‌اعتمادتر بود
- **اعلان‌های خودکار در کانال:** فعال‌شدن Entry اول/دوم، تاچ هر تارگت (با درصد سود دقیق بر اساس لوریج)، فعال‌شدن Stop Loss یا تریلینگ استاپ
- **تریلینگ استاپ واقعی:** بعد از هر تارگت، استاپ به تارگت قبلی (یا نقطه ورود) منتقل می‌شود؛ نسبت بستن پوزیشن: تک‌تارگت ۱۰۰٪، دو‌تارگتی ۵۰٪/۵۰٪، سه‌تارگتی ۵۰٪/۲۵٪/۲۵٪
- **گزارش عملکرد خودکار:** بعد از هر ۵ سیگنال کامل‌شده، یک تصویر گزارش حرفه‌ای (با فونت‌های سفارشی) در کانال پست می‌شود
- **ذخیره‌سازی:** به‌جای دیتابیس، از دو فایل JSON ساده استفاده می‌شود (`active_signals.json`, `signal_history.json`) — این فایل‌ها با هر دیپلوی روی سرویس‌های بدون فضای دائمی (مثل Railway بدون Volume) از بین می‌روند، ولی چون داده‌های سیگنال برای شما اهمیت حیاتی ندارند، این مشکلی ایجاد نمی‌کند

## ۴. مراحل دیپلوی از صفر (روی هر هاستی)

۱. یک ریپازیتوری جدید در GitHub بسازید (Private)
۲. این ۵ فایل/پوشه (بخش ۱) را در آن آپلود کنید
۳. در Railway (یا هر سرویس مشابه)، پروژه جدید بسازید و همان ریپازیتوری را وصل کنید
۴. سه متغیر محیطی (بخش ۲) را تنظیم کنید
۵. صبر کنید دیپلوی انجام شود؛ در Deploy Logs باید ببینید ربات polling را شروع کرده
۶. با اکانت ادمین، `/start` را در ربات بزنید تا مطمئن شوید پاسخ می‌دهد

## ۵. نکات مهمی که در طول مسیر یاد گرفتیم (برای جلوگیری از تکرار اشکالات قبلی)

- اگر همزمان روی دو پلتفرم (مثلاً هم Replit هم Railway) با یک توکن اجرا شود، خطای `TelegramConflictError` می‌دهد — همیشه نسخه‌ی قدیمی را قبل از فعال‌کردن نسخه‌ی جدید متوقف کنید
- Railway حساب‌های رایگان معمولاً بعد از ۳۰ روز یا مصرف ۵ دلار اعتبار اولیه، به پلن محدودتری می‌روند؛ برای اجرای بی‌وقفه، پلن Hobby (حدود ۵ دلار/ماه) نیاز است
- دستورهای `/backup` و `/restore` به ربات اضافه شده‌اند تا در صورت نیاز، فایل‌های JSON را از تلگرام بگیرید یا برگردانید (اختیاری، چون گفتید این دیتا اولویت نیست)
